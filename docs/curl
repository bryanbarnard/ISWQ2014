## Curl Commands Based Tests ##

# HEAD ROOT
curl -iL http://localhost:3000 \
 -X HEAD \
 -H 'Accept: text/plain' \
 -H 'Content-Type: text/plain' \
 -H 'Connection: close'

# GET ROOT
curl -iL http://localhost:3000 \
 -X GET \
 -H 'Accept: text/plain' \
 -H 'Content-Type: text/plain' \
 -H 'Connection: close'

# GET Invalid URL
curl -iL http://localhost:3000/test \
 -X GET \
 -H 'Accept: text/plain' \
 -H 'Content-Type: text/plain' \
 -H 'Connection: close'

# GET ROOT Custom Content Type
curl -iL http://localhost:3000 \
-X GET \
-H 'Connection: close' \
-H 'Content-Type: application/vnd.collection+json'

# New Entry for Test Post Request